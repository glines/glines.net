srcdir = @srcdir@
top_srcdir = @top_srcdir@
VPATH = @srcdir@

all: build

.PHONY: build
build:
	mkdir -p ./build
	cd $(srcdir)/repo && git archive --format=tar HEAD \
		| (cd $(shell pwd)/build/ && tar xf -)

clean:
	rm -rf ./build
