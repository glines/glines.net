srcdir = @srcdir@
top_srcdir = @top_srcdir@
VPATH = @srcdir@

all: build

.PHONY: build
build:
	mkdir -p ./build
	cd repo && git archive --format=tar HEAD | (cd ../build/ && tar xf -)

clean:
	rm -rf ./build
